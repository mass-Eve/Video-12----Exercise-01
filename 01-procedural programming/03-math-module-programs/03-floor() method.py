import math

decimal_value = float(input("Enter a decimal value to get its ceil value: "))

print(f"{decimal_value} on rounding down to the next closest integer comes out as {math.floor(decimal_value)}")